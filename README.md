# SysteM

Single source of truth for App Capital infrastructure.

## Structure

- `terraform/` — VPS and DNS provisioning
- `ansible/` — VM bootstrap and k3s installation
- `gitops/` — ArgoCD manifests for all apps and infra
- `docs/` — Architecture decisions and runbooks
