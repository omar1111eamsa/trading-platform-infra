[defaults]
inventory           = inventories/ovh/hosts.yml
remote_user         = ubuntu
private_key_file    = ~/.ssh/id_rsa
host_key_checking   = False
retry_files_enabled = False
stdout_callback     = yaml
collections_path    = ./collections

[privilege_escalation]
become      = True
become_method = sudo
become_user = root
