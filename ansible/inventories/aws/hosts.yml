all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: /home/omar/stage-tradingplatform/SysteM/terraform-aws/trading-aws-test.pem
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o IdentitiesOnly=yes -o IdentityAgent=none'
  children:
    linux_api:
      hosts:
        aws-linux-node:
          ansible_host: REDACTED_IP
    linux_ui:
      hosts:
        aws-linux-node:
          ansible_host: REDACTED_IP
    linux_dashboard:
      hosts:
        aws-linux-node:
          ansible_host: REDACTED_IP
    workers:
      hosts: {}
