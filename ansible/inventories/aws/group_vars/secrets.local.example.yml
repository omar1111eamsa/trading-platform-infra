# Copy to secrets.local.yml and fill real values.

github_pat: "ghp_xxx"

# Generate per-repo from GitHub API/CLI before running playbooks.
backend_github_runner_registration_token: ""
frontend_github_runner_registration_token: ""
dashboard_github_runner_registration_token: ""

certbot_admin_email: "admin@example.com"

backend_env:
  jwt_secret: "replace-with-strong-secret"
  admin_username: "admin"
  admin_email: "admin@example.com"
  admin_password: "REDACTED_PASSWORD"
  influxdb_token: "replace-with-strong-token"

ui_terminal_env:
  next_public_api_url: "https://api.example.com"
  next_public_ws_url: "wss://api.example.com/ws"
