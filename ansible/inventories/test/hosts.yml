all:
  children:
    linux_ui:
      hosts:
        trading-platform-vm-ui:
          # Lab placeholder; with Terraform use `-i inventories/aws/hosts.generated.yml` after this file so the EIP wins.
          ansible_host: 203.0.113.10
    linux_api:
      hosts:
        trading-platform-vm-api:
          ansible_host: 203.0.113.10
    linux_dashboard:
      hosts:
        trading-platform-vm-dashboard:
          ansible_host: 203.0.113.10
    workers:
      hosts: {}
