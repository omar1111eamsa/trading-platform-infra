- name: Prepare backend VM
  hosts: backend
  become: true
  pre_tasks:
    - name: Load local test secrets when present
      ansible.builtin.include_vars:
        file: "../inventories/test/group_vars/secrets.local.yml"
  roles:
    - common
    - docker
    - deploy_user
    - app_checkout
    - app_env
