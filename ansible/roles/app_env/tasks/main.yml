- name: Render application environment file
  ansible.builtin.template:
    src: "{{ env_template }}"
    dest: "{{ env_dest }}"
    owner: "{{ deploy_user }}"
    group: "{{ deploy_group }}"
    mode: "0600"
