services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    ports:
      - "{{ monitoring_port }}:3001"
    volumes:
      - uptime_kuma_data:/app/data
    restart: unless-stopped

volumes:
  uptime_kuma_data:
