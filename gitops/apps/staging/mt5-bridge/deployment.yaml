apiVersion: apps/v1
kind: Deployment
metadata:
  name: mt5-bridge
  namespace: staging
  labels:
    app: mt5-bridge
    env: staging
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mt5-bridge
  template:
    metadata:
      labels:
        app: mt5-bridge
        env: staging
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: mt5-bridge
          image: ghcr.io/trading-platform/mt5-bridge:db309924c9a3d124f7e10c6e19f0d31833a50243
          imagePullPolicy: Always
          ports:
            - containerPort: 5556
              hostPort: 5556
          envFrom:
            - secretRef:
                name: mt5-bridge-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: ghcr-credentials
