apiVersion: apps/v1
kind: Deployment
metadata:
  name: mt5-bridge
  namespace: staging
  labels:
    app: mt5-bridge
    env: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mt5-bridge
  template:
    metadata:
      labels:
        app: mt5-bridge
        env: staging
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: mt5-bridge
          image: ghcr.io/trading-platform/mt5-bridge:a5bf8c8f2f951867dcf53707f88cc2cc6e74c1fe
          imagePullPolicy: Always
          ports:
            - containerPort: 5556
          envFrom:
            - secretRef:
                name: mt5-bridge-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: ghcr-credentials
