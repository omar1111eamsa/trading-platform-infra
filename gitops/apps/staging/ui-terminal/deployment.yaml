apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui-terminal
  namespace: staging
  labels:
    app: ui-terminal
    env: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui-terminal
  template:
    metadata:
      labels:
        app: ui-terminal
        env: staging
    spec:
      containers:
        - name: ui-terminal
          image: ghcr.io/trading-platform/ui-terminal:7f561598ad4f3f63ba32001ad14c281a7f414933
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: ui-terminal-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: ghcr-credentials
